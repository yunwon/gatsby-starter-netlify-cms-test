import React, { Component } from "react";
import styles from "./Support.module.scss";
import { LayoutCn } from "src/layout/LayoutCn";

export default class SupportCn extends Component {
  render() {
    return (
      <LayoutCn>
        <div>Support Chinese Page</div>
      </LayoutCn>
    );
  }
}
